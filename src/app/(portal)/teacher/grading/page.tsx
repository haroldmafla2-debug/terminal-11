import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function TeacherGradingPage() {
  return (
    <FeaturePlaceholder
      title="Docente - Calificar"
      description="Libro de notas base con edición y validación (PR4 completo)."
    />
  );
}
